=== 2026-09-10 | session 567fdf5c (seeded retroactively from the session transcript; original timestamps unavailable) ===
/init

=== 2026-09-10 | session 567fdf5c (seeded retroactively from the session transcript; original timestamps unavailable) ===
Act as a Software Architect and Lead Technical Artist, expert in Three.js, Vite, and Node.js.
I need to design the core architecture for a hyper-casual puzzle web minigame. The game must be built initially using 3D primitives. The architecture must be highly scalable, ensuring that the core game logic is strictly decoupled from the visual rendering layer to easily allow for future visual upgrades or reskins and robust unit testing.
IMPORTANT: I DO NOT want you to write the actual functional implementation or mathematical logic for the game. I ONLY want the structural blueprint, class outlines (API, properties, method signatures), and architectural design.
Technical requirements:

* Stack: Node.js (LTS), Vite, Three.js (Vanilla).
* Language: JavaScript ONLY (modular, clear patterns like Managers). No TypeScript.
* Testing: Vitest (or Jest) for unit testing the decoupled game logic.
* Parameterization: Use a central `Config.js` object. All dimensions, speeds, slot limits, and timings must be highly parameterizable.

Game Mechanics & Architecture Details:

1. Level Design & Grid: The `GridManager` must build the board by parsing 2D arrays (matrices where e.g., 0 = empty, 1 = color A, 2 = color B). It must support 0 padding between blocks.
2. Mechanics: Units consume blocks of their own color from the outermost edge of the grid. They ignore opposite colors or empty rows. Capacity decreases by 1 per block.
3. Interaction & Inventory (CRITICAL): A reserve inventory of 4 columns x N rows. An active playing zone with a strict limit of 5 slots. Clicking a reserve unit moves it to an active slot, and it starts its track run. If capacity hits 0, it dies (freeing the slot). If it completes the loop with capacity > 0, it returns to its slot, permanently blocking it.
4. Camera: Strictly `OrthographicCamera` (top-down) to flatten the 3D space.
5. The Track: A fixed, one-way outer perimeter loop. Units NEVER enter the grid; they travel the outer track and scan inwards to clear blocks.
6. Win/Loss: Win if the matrix is entirely 0. Lose if all 5 active slots are blocked and no valid moves remain.

Your task:

1. Propose the code architecture: Explain your chosen patterns (e.g., GameManager, GridManager, UIManager) to ensure logic and rendering are separated and highly scalable.
2. Generate the structural outlines (No implementation): Provide the class skeletons for the core logic files (`Config.js`, `GameManager.js`, `GridManager.js`, `InventoryManager.js`). Show only properties and public methods. Show how `GridManager` ingests the 2D array.
3. Define the Renderer interface: Show the skeleton of `Renderer.js` acting as the bridge to Three.js (e.g., `initOrthographicCamera()`, `buildGridFromState()`).
4. AI logging hook: Create a hook so that the prompts I provide are stored in /ai_logs/*.txt.
5. Unit Testing Skeleton: Provide the skeleton for a unit test file (e.g., `GridManager.test.js` or `GameManager.test.js`) demonstrating how this decoupled logic can be easily tested using Vitest/Jest without needing to render the Three.js scene.

=== 2026-09-10T18:22:08-05:00 | session 567fdf5c ===
Read CLAUDE.md and all existing skeletons, then write docs/IMPLEMENTATION_PLAN.md to turn them into a fully playable v1 primitive blockout (grid, track, units, inventory, win/loss) without changing the architecture. STOP for my approval.
Rules: src/core stays pure and deterministic (no three, DOM, clocks or Math.random), every value lives in Config.js, and the renderer only reads getState() snapshots and events.
After approval, implement milestone by milestone: core logic → headless tests that play one level to WIN and one to LOSE → renderer, input and UI. Replace it.todo with real tests and keep npm test green.
Stop after each milestone with a short report and a proposed commit message. Ask me before adding dependencies or whenever a rule is ambiguous.

=== 2026-09-10T21:08:28-05:00 | session 567fdf5c ===
go

=== 2026-09-10T21:31:34-05:00 | session 567fdf5c ===
go

